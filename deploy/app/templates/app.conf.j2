server
{
  listen 80;

  server_name 127.0.0.1;

  root /usr/share/nginx/html/app;
  index index.html;

  location /static
  {
    alias /opt/app/;
    expires 30d;
  }

  error_page 404 =200 @error;

  location @error
  {
   index error.html;
   rewrite ^ /;
  }

}
